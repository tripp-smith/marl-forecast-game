global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "ray"
    static_configs:
      - targets: ["ray-head:8080"]

  - job_name: "marl_app"
    static_configs:
      - targets: ["app:9090"]

rule_files:
  - "alert_rules.yml"
